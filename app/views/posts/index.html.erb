<h1>Feed</h1>

<div class="row">
  <div class="col-md-12 text-center">
    <%= link_to 'Add a new post', new_post_path, class: 'btn btn-primary' %>
  </div>

  <div class="col-md-12">
      <h2><%= empty_posts(@posts) %></h2>
  </div>

  <% @posts.each do |post| %>
    <div class="col-md-12">
      <%= render post %>
    </div>
  <% end %>
</div>
